import { test } from '@playwright/test';

test.describe.serial('Search', () => {
  test('Code search', () => {});
  test('Code filters search', () => {});
  test('Symbol search', () => {});
  test('Repo search', () => {});
  test('Path search', () => {});
  test('Repo navigate from search', () => {});
  test('File navigate from search', () => {});
});
